<div class="home row">
	<div class="col-md-6 text-right">
		<%= image_tag("karl1.jpeg", class:'img-home') %>
	</div>
	<div class="news col-md-6">
		<h4 class="text-center">News</h4>
		<% unless @headlines.size == 0 %>
			<% @headlines.each do |headline| %>
				<p class="small"><%= headline.created_at.strftime("%m/%d/%Y") %></p>
				<p><%= headline.title %>
					<% if policy(Headline.new).destroy? %>
						<%= link_to "DELETE", headline_path(headline), method: :delete %>
					<% end %>
				</p>
			<% end %>
		<% else %>
			<p>No news yet!</p>
		<% end %>
		<% if policy(Headline.new).create? %>
			<%= link_to "Add", new_headline_path, class:'btn' %>
		<% end %>
	</div>
</div>
